import {COMBOS,PERSONAL,LAYOUTS} from './expansion-data.js?v=0.44.0';import {personalReason} from './ventures.js?v=0.44.0';
export function expansionPanel(s,d,esc,btn,includeDiplomacy=true){const x=s.expansion;if(!x)return '<section class="realm-section" id="realm-expansion"><h2>军需与战役</h2><p>开启组合配合、专属任务、主动防守和多队接力。旧存档不追溯发放物资。</p>'+btn('开启军需与战役',{type:'expansionOpen'},'primary')+'</section>';if(x.run){const r=x.run;return '<section class="realm-section"><h2>'+(r.kind==='defense'?'三波寨防':'三队接力')+' · '+r.stage+' / 3</h2><p>暂存碎银 '+r.loot.silver+'、精铁 '+r.loot.iron+'。完成三场额外获得军需自选箱 1；提前收队仅带回已暂存所得的一半。</p>'+(r.kind==='defense'?'<p>布防：'+LAYOUTS[r.layout].name+'；'+r.team.map(id=>d.by.heroes[id].name+' '+(r.hp[id]/100).toFixed(1)+'%').join('、')+'</p>':'<p>本轮已出阵：'+(r.used.map(id=>d.by.heroes[id].name).join('、')||'暂无')+'。</p><p>每场可用 1—3 位尚未出阵好汉，在出征准备中调整；同一人本轮只能出战一次。</p>')+'<p>下一场粮耗为 5 加实际兵力五分之一向上取整；不另扣体力。</p>'+btn('准备下一场',{type:'ventureNext'},'primary',r.stage>=3)+btn(r.stage===3?'完成战役并领取':'提前收队',{type:'ventureReturn'},'secondary')+'</section>';}
 return '<section class="realm-section" id="realm-expansion"><h2>军需与战役</h2><p>烟幕包 ×'+(s.inventory.smoke_pack||0)+'；当前'+(x.smoke?'已安排携带下一场':'未携带')+'。携带后新战斗出发即消耗，不返还；仅主动撤退减少 25% 兵损。</p>'+btn(x.smoke?'取消携带烟幕':'下次出征携带烟幕',{type:'smokeReady'},'secondary',!s.inventory.smoke_pack)+'<h3>组合羁绊</h3><p>组合成员共同取胜三次解锁合击，每场最多两次，间隔至少 15 秒；触发时每人消耗怒气 20，可能推迟个人技能。</p>'+Object.entries(COMBOS).map(([id,m])=>'<p><b>'+m.name+'</b>（'+(x.combos[id]||0)+' / 3）：'+m.team.map(id=>d.by.heroes[id].name).join('、')+'。'+m.text+'</p>').join('')+'<h3>专属实战任务</h3><p>本人 10 级、聚义厅 2 级；今日已尝试 '+(s.daily.counters.personal_attempt||0)+' / 3 次，消耗体力 10、粮草 5 加随军人数一半。完成获得本人招式书 2、灵蕴 2，并解锁专属强化，仅一次。白胜、安道全和鲁智深任务在本人超过 15 级后提升敌军属性，奖励不随等级增加。</p><div class="realm-grid">'+Object.entries(PERSONAL).map(([id,m])=>'<details class="realm-card" data-fold="personal-'+id+'"><summary>'+d.by.heroes[id].name+' · '+m.name+(x.personal[id]?' · 已完成':'')+'</summary><p>'+m.text+'</p><p>强化：'+m.reward+'</p><p>'+esc(x.personal[id]?'任务已完成，本领强化已生效。':personalReason(s,id))+'</p>'+btn('准备专属任务',{type:'personalStart',id},'secondary',!!x.personal[id]||s.heroes[id]?.status!=='owned'||s.heroes[id].level<10||s.camp.buildings.hall<2||(s.daily.counters.personal_attempt||0)>=3)+'</details>').join('')+'</div><h3>主动寨防 · 三波来敌</h3><p>聚义厅 3 级，每日一次。启程需体力 15、粮草 20、木材 40、碎银 80。四种布防任选一种，队伍与气血连续保留；离线不遇袭。</p>'+Object.entries(LAYOUTS).map(([layout,m])=>'<p>'+m.name+'：'+m.text+'</p>'+btn('采用'+m.name+'并开始',{type:'ventureStart',kind:'defense',layout},'secondary',s.camp.buildings.hall<3||!!s.daily.counters.venture_defense)).join('')+'<h3>多队接力 · 前哨、水路、主寨</h3><p>聚义厅 3 级，每日一次。启程体力 15、粮草 20。三场不能重复用人，沿用实际兵力与兵损；可中途结算退出。</p>'+btn('开始多队接力',{type:'ventureStart',kind:'relay'},'primary',s.camp.buildings.hall<3||!!s.daily.counters.venture_relay)+'</section>'+(includeDiplomacy?diplomacyPanel(s,btn):'');}
export function diplomacyPanel(s,btn){return '<section class="realm-section"><h2>势力抉择 · 每地每月一次</h2><p>三方都请求山寨相助。支持一方关系 +12，其余两方各 −4。若选择当地势力，本月当地委托战开场全队怒气 +5。选择立即生效，本月不能改选。</p><div class="realm-grid">'+Object.entries({yuncheng:'郓城',jingyang:'景阳',dongxi:'东溪'}).map(([id,name])=>{const choice=s.expansion.diplomacy[id+'_'+s.daily.date.slice(0,7)],labels={merchants:'商会：银 100 换粮 60',villages:'乡村：粮 40 换药草 5',river:'船帮：木 30 换粗布 3'};return '<article class="realm-card"><h3>'+name+'</h3><p>'+(choice?'已支持 '+labels[choice]:'尚未选择')+'</p>'+Object.entries(labels).map(([choice,label])=>btn(label,{type:'diplomacyChoose',id,choice},'secondary',!!s.expansion.diplomacy[id+'_'+s.daily.date.slice(0,7)])).join('')+'</article>';}).join('')+'</div></section>';}

export function personalTaskPanel(s,d,h,btn){const m=PERSONAL[h.id];if(!m||s.heroes[h.id]?.status!=='owned')return '';return '<section class="realm-section"><h3>专属实战 · '+m.name+'</h3><p>'+m.text+'</p><p>完成后：'+m.reward+'</p>'+ (m.scaling?'<p class="note">本人超过 15 级后敌军属性随等级提高，奖励固定；治疗与承伤任务可搭配护阵、治疗同伴，速战任务可准备回气散。</p>':'')+'<p>本人 10 级、聚义厅 2 级；体力 10，粮草 5 加兵力一半。'+(s.expansion?.personal[h.id]?'已经完成。':'今日已尝试 '+(s.daily.counters.personal_attempt||0)+' / 3 次。')+'</p>'+(!s.realm?'<p>先开启山河经营，即可准备此任务。</p>'+btn('开启山河经营',{type:'realmOpen'},'secondary',!s.camp):'')+btn('准备专属任务',{type:'personalStart',id:h.id},'secondary',!s.realm||s.camp.buildings.hall<2||s.heroes[h.id].level<10||!!s.expansion?.personal[h.id]||(s.daily.counters.personal_attempt||0)>=3)+'</section>';}

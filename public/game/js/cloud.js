import { hasSixthVolume } from './volume-six-data.js?v=0.32.0';
import { hasFifthVolume } from './volume-five-data.js?v=0.32.0';
import { hasFourthVolume } from './volume-four-data.js?v=0.32.0';
import { hasThirdVolume } from './volume-three.js?v=0.32.0';
import { gameSnapshot, slotId, slotName } from './portable.js?v=0.32.0';
const revision=n=>{if(!Number.isSafeInteger(n)||n<0)throw new Error('云端版本格式无效。');return n;};
const listing=s=>({id:slotId(s.id),name:slotName(s.name),public:s.public===true});
export class CloudError extends Error { constructor(message,status,details={}){super(message);this.status=status;Object.assign(this,details);} }
export class CloudClient {
  constructor(baseUrl,data,fetcher=(...args)=>fetch(...args)){
    this.data=data;this.fetcher=fetcher;this.keys=new Map();this.baseUrl='';
    if(baseUrl){const url=new URL(baseUrl);if(url.protocol!=='https:'||url.username||url.password||url.search||url.hash||url.pathname!=='/')throw new Error('云端地址须为无路径、无凭据的 HTTPS 来源。');this.baseUrl=url.origin;}
  }
  setKey(id,key){id=slotId(id);key=String(key||'').trim();if(key)this.keys.set(id,key);}
  forget(){this.keys.clear();}
  async request(path,{method='GET',id,key,revision,body}={}){
    if(!this.baseUrl)throw new Error('云端服务尚未部署或配置；本机存档与文件备份仍可使用。');
    const headers={};if(body)headers['Content-Type']='application/json';
    const token=key??this.keys.get(id);if(token)headers.Authorization='Bearer '+token;
    if(revision!==undefined)headers['If-Match']=`"${revision}"`;
    const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),15000);
    try{
      const res=await this.fetcher(this.baseUrl+path,{method,headers,body:body?JSON.stringify(body):undefined,cache:'no-store',credentials:'omit',redirect:'error',referrerPolicy:'no-referrer',signal:controller.signal});
      const value=await res.json();if(!res.ok)throw new CloudError(String(value.error).includes('好汉字典')?'云端存档名册与本机不一致，请先更新云存档服务；本机进度已保留，可导出文件备份。':value.error||'云端操作失败。',res.status,{cloudRevision:value.cloudRevision});return value;
    }catch(e){if(e instanceof CloudError)throw e;throw new Error('无法确认云端结果（网络、超时或地址配置问题）。不要反复覆盖提交；请先查看云端，再接续或重试。');}
    finally{clearTimeout(timer);}
  }
  async list(){const r=await this.request('/v1/slots');if(!Array.isArray(r.slots)||r.slots.length!==20)throw new Error('云端列表格式无效。');const slots=r.slots.map(listing);if(new Set(slots.map(s=>s.id)).size!==20)throw new Error('云端编号重复。');return {slots};}
  async download(id){id=slotId(id);const r=await this.request(`/v1/slots/${id}`,{id});if(r.id!==id)throw new Error('云端编号不匹配。');return {...listing(r),cloudRevision:revision(r.cloudRevision),updatedAt:r.updatedAt,state:r.state?gameSnapshot(r.state,this.data):null};}
  async leaderboard(){const r=await this.request('/v1/leaderboard');if(!/^\d{4}-\d{2}-[1-4]$/.test(r.period)||!Array.isArray(r.entries)||r.entries.length>20||r.entries.some(e=>!Number.isInteger(e.id)||e.id<1||e.id>20||!Number.isInteger(e.tier)||e.tier<1||e.tier>5||!Number.isInteger(e.score)||e.score<1000000||e.score>5100180||!Number.isInteger(e.rank)||e.rank<1||e.rank>20))throw new Error('排行榜数据无效。');return {period:r.period,entries:r.entries.map(({id,tier,score,rank})=>({id,tier,score,rank}))};}
  async verifiedLeaderboard(group='open'){const r=await this.request('/v1/verified-leaderboard'+(group==='open'?'':'?group='+encodeURIComponent(group)));if(group!=='open'&&r.group!==group)throw new Error('云端尚未支持分组榜，请更新存档服务。');if(r.verified!==true||!/^\d{4}-\d{2}-[1-4]$/.test(r.period)||!Array.isArray(r.entries)||r.entries.length>20||r.entries.some(e=>!Number.isInteger(e.id)||e.id<1||e.id>20||!Number.isInteger(e.tier)||e.tier<1||e.tier>5||!Number.isInteger(e.score)||e.score<1000000||e.score>5100180||!Number.isInteger(e.rank)||e.rank<1||e.rank>20))throw new Error('服务器演武榜格式无效。');return r;}
  async verify(id,rev,tier,group='open'){if(group!=='open'){const v=await this.request('/v1/game-version');if(v.challengeGroups!==1)throw new Error('请先更新云存档服务至 0.28.0，再参加分组演武。');}const r=await this.request('/v1/slots/'+slotId(id)+'/verify',{id,method:'POST',revision:rev,body:{tier,...(group==='open'?{}:{group})}});if(group!=='open'&&r.group!==group)throw new Error('云端尚未支持分组演武，请更新存档服务。');if(r.verified!==true||r.id!==id||!['victory','defeat','retreat'].includes(r.outcome))throw new Error('演武结果格式无效。');return r;}
  async cooperative(id){const r=await this.request('/v1/slots/'+slotId(id)+'/cooperative',{id});if(r.id!==id||!Array.isArray(r.stages)||!Number.isInteger(r.total))throw Error('协作进度格式无效。');return r;}
  async cooperate(id,rev){const version=await this.request('/v1/game-version');if(version.cooperative!==1)throw Error('请更新 0.29.0 云服务并安装协作数据表。');this.coopRequests??=new Map();let requestId=this.coopRequests.get(id);if(!requestId){requestId=Array.from(crypto.getRandomValues(new Uint8Array(16)),v=>v.toString(16).padStart(2,'0')).join('');this.coopRequests.set(id,requestId);}const r=await this.request('/v1/slots/'+slotId(id)+'/cooperate',{id,method:'POST',revision:rev,body:{requestId}});this.coopRequests.delete(id);return r;}
  claimCooperative(id,rev,period,stage){return this.request('/v1/slots/'+slotId(id)+'/coop-claim',{id,method:'POST',revision:rev,body:{period,stage}});}
  async upload(id,revision,state,name){const expanded=(this.data.config.rosterVersion||3)>3,fifth=hasFifthVolume(state),fourth=hasFourthVolume(state),third=hasThirdVolume(state),elite=state.battle?.context.type==='elite'||state.lastBattle?.context.type==='elite',reports=state.lastBattle||state.battle?.metrics,affairs=state.affairs,strategy=state.strategy,commands=state.commandVersion===1,frontier=state.frontier||state.battle?.frontierRules,advanced=state.development||state.battle?.martial===2,rotations=state.campaign||state.battle?.martial||state.camp?.arm;if(state.recruit.support||expanded||fifth||fourth||third||elite||reports||affairs||strategy||commands||frontier||advanced||rotations){const version=await this.request('/v1/game-version');if(hasSixthVolume(state)&&!(version.chapters>=6))throw new Error('请先更新云存档服务至 0.25.0，再上传第六卷进度。本机进度已保留。');if(fifth&&!(version.chapters>=5))throw new Error('请先更新云存档服务至 0.23.0，再上传第五卷路线、攻庄准备和战报。本机进度已保留。');if(fourth&&!(version.chapters>=4))throw new Error('请先更新云存档服务至 0.22.0，再上传第四卷地图、山盟选择和战报。本机进度已保留。');if(third&&!(version.chapters>=3))throw new Error('请先更新云存档服务至 0.21.0，再上传第三卷地图、剧情和战报。本机进度已保留。');if(elite&&version.elite!==1)throw new Error('请先更新云存档服务至 0.20.0，再上传精英战局与复盘。本机进度已保留。');if(commands&&version.commands!==1)throw new Error('请先更新云存档服务至 0.12.0，再上传军令、十连与装备锁定进度。本机进度已保留。');if(frontier&&version.frontier!==1)throw new Error('请先更新云存档服务至 0.11.0，再上传领地生产与新战法进度。本机进度已保留。');if(advanced&&version.development!==1)throw new Error('请先更新云存档服务至 0.10.0，再上传专属兵种与阵容进度。本机进度已保留。');if(rotations&&version.rotations!==1)throw new Error('请先更新云存档服务至 0.9.0，再上传轮换历练与兵种进度。本机进度已保留。');if(affairs&&version.management!==1)throw new Error('请先更新云存档服务至 0.14.0，再上传寨事和外派进度。本机进度已保留。');if(strategy&&version.strategy!==1)throw new Error('请先更新云存档服务至 0.13.0，再上传部队专精和副本机制进度。本机存档已保留。');if(reports&&version.reports!==1)throw new Error('请先更新云存档服务至 0.16.0，再上传战斗复盘记录。本机进度已保留。');if(expanded&&(!(version.rosterVersion>=(this.data.config.rosterVersion||3))||!(version.heroes>=this.data.heroes.length)))throw new Error('请先更新云存档服务至 '+this.data.config.release+'，再上传新人物名册及新兵员规则。本机人物进度已保留。');if(state.battle?.expedition?.fieldRules===2&&version.fieldRules!==2)throw new Error('请先更新云存档服务至 0.26.0，再上传新战局。本机进度已保留。');if(state.frontier?.focus!==undefined&&version.productionFocus!==1)throw new Error('请先更新云存档服务至 0.27.0，再上传经营侧重。本机进度已保留。');if(state.campaign?.mastery!==undefined&&version.materialSweeps!==1)throw new Error('请先更新云存档服务至 0.27.0，再上传材料本熟练记录。本机进度已保留。');if((state.expansion||['ration','vital_pill','camp_pack','medical_crate','smoke_pack','material_choice'].some(id=>state.inventory[id]>0))&&version.expansion!==1)throw new Error('请先更新云存档服务至 0.29.0，再上传补给与新战役。本机进度已保留。');if(state.realm&&version.realm!==1)throw new Error('请先更新云存档服务至 0.28.0，再上传山河经营、分队和远征进度。本机进度已保留。');if(state.recruit.support&&version.recruitSupport!==1)throw new Error('请先更新云存档服务至 0.30.0，再上传招贤保障与补给进度。本机进度已保留，无需新增数据库表。');}id=slotId(id);return this.request(`/v1/slots/${id}`,{id,method:'PUT',revision,body:{state:gameSnapshot(state,this.data),name}});}
  sharing(id,revision,value){return this.request(`/v1/slots/${slotId(id)}/sharing`,{id,method:'POST',revision,body:{public:value}});}
  async history(id){id=slotId(id);const r=await this.request(`/v1/slots/${id}/history`,{id});if(r.id!==id||!Array.isArray(r.history)||r.history.length>10)throw new Error('云端历史格式无效。');return {id,cloudRevision:revision(r.cloudRevision),history:r.history.map(h=>({cloudRevision:revision(h.cloudRevision),name:slotName(h.name),updatedAt:String(h.updatedAt||'')}))};}
  rollback(id,revision,old){return this.request(`/v1/slots/${slotId(id)}/rollback`,{id,method:'POST',revision,body:{revision:old}});}
  admin(id,action,key,revision){return this.request(`/v1/admin/slots/${slotId(id)}/${action}`,{method:'POST',key,revision});}
}

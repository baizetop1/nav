import { workshopQuote, RESOURCE_NAMES } from './production.js?v=0.31.0';
export function workshopOrders(s,btn){const q=workshopQuote(s);return '<section class="workshop-orders"><h3>工坊订单 · 按需加工</h3><p class="note">每批消耗木材 2、碎铁 2，获得精铁 1；不消耗碎银、体力。未下单的额度会保留。</p><p>当前额度 '+q.bank+' 批 · 原料和库存允许加工 '+q.available+' 批</p><p class="note">现有木材 '+(s.camp?.wood||0)+' · 碎铁 '+(s.inventory.scrap_iron||0)+' · 精铁 '+(s.inventory.iron||0)+'</p>'+(q.reason?'<p class="note">'+q.reason+'</p>':'')+'<div class="actions">'+[1,5,10,'all'].map(count=>btn(count==='all'?'预览全部可加工':'预览加工 '+count+' 批',{type:'ui_productionPreview',kind:'workshop',count},'secondary',!!q.reason)).join('')+'</div><p class="note">数量超过当前条件时会先缩减预览，不会直接扣料。碎铁可从矿山、每日铁石山道或装备分解获得；先收取原料再下单。</p></section>';}
export function productionDialog(q,esc,btn){
 const collect=q.kind==='collect';
 const notes=collect?(Object.entries(q.left).map(([id,n])=>RESOURCE_NAMES[id]+'库存将满，'+n+' 份仍留在产地。').concat(q.blocked.length?'以下据点待解围：'+q.blocked.join('、')+'。':[])).map(t=>'<p class="note">'+esc(t)+'</p>').join(''):'';
 const command=collect?{type:'frontierCollect',expected:q.signature}:{type:'frontierProcess',count:q.count,expected:q.signature};
 return '<dialog id="production-preview" aria-labelledby="production-title"><p class="kicker">寨务账房 · 确认后办理</p><h2 id="production-title" tabindex="-1">'+(collect?'收取已产物资':'工坊加工订单')+'</h2>'+(!collect?'<p>计划 '+(q.requested==='all'?'全部可加工':q.requested)+' 批，本次可加工 '+q.count+' 批。</p>':'<p class="note">仅收取农田、伐木场和畅通据点的产出，工坊原料与额度不动。</p>')+(q.reason?'<p class="notice error" role="status">'+esc(q.reason)+'</p>':'')+'<div class="batch-changes">'+q.rows.map(r=>'<p><span>'+esc(r.name)+'</span><b>'+esc(r.value)+'</b></p>').join('')+'</div>'+notes+'<p class="note">确认前不修改物资；取消保留全部进度。</p><div class="actions">'+btn(collect?'确认收取':'确认加工 '+q.count+' 批',command,'primary',!!q.reason)+btn('取消',{type:'ui_productionCancel'},'secondary')+'</div></dialog>';
}

# 白泽 - 个人导航 (Next Gen)

这是一个基于 React + Vite + Tailwind CSS 的现代化个人导航主页。

## ✨ 核心特性

- **🌅 动态渐变背景**:
  - 根据时间自动切换背景色调（白天：清新蓝紫；夜间：深邃星空）
  - 支持一键切换自动/手动渐变模式
  - 侧边栏与主内容区拥有独立的渐变体系，视觉层次丰富
- **🎨 极致视觉体验**:
  - 全局 Glassmorphism（毛玻璃）设计风格
  - 顶部导航栏与侧边栏采用高透视设计，完美融合背景
  - 精心调教的字体阴影与背景胶囊，确保在任何背景下文字都清晰可读
- **� 安全与管理**:
  - 内置简易后台管理功能（添加/导出/重置数据）
  - 环境变量控制鉴权，无需担心密码泄露
  - 支持本地存储（LocalStorage）持久化保存自定义数据
- **🔍 智能搜索**:
  - 支持多引擎快速切换（Google, Baidu, Bing, GitHub 等）
  - 键盘快捷键支持（Cmd/Ctrl + K 唤起搜索）
- **🌓 深度暗黑模式**:
  - 完美适配系统深色模式
  - 独立的夜间渐变配色方案
- **📱 全端响应式**:
  - 移动端适配侧边栏抽屉与触摸交互
  - PC 端沉浸式大屏体验

## 🛠️ 技术栈

- **Core**: React 18 + TypeScript
- **Build**: Vite 5
- **Styling**: Tailwind CSS + Lucide React Icons
- **Deployment**: GitHub Actions (自动构建部署)

## 🚀 快速开始

1. **安装依赖**:
   ```bash
   npm install
   ```

2. **配置环境变量**:
   复制 `.env.example` 为 `.env` 并设置你的管理密码：
   ```bash
   VITE_AUTH_TOKEN=你的Base64加密密码
   ```

3. **启动开发服务器**:
   ```bash
   npm run dev
   ```

4. **构建生产版本**:
   ```bash
   npm run build
   ```

## 📂 数据管理

所有默认网站数据都存储在 `src/data.ts` 中。你可以直接修改该文件来初始化你的导航内容。
同时，用户在网页端添加的自定义网站会保存在浏览器的 LocalStorage 中。

## 📦 部署

本项目配置了 GitHub Actions 自动部署。
只需将代码推送到 `main` 分支，GitHub Actions 会自动构建并发布到 `gh-pages` 分支。

**注意**：为了使在线管理功能生效，请在 GitHub 仓库的 Settings -> Secrets 中添加 `VITE_AUTH_TOKEN` 环境变量。

## 📄 License

MIT License
